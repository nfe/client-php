# Copy to .env so docker-compose picks the host UID/GID for file ownership.
#
#   cp .env.docker.example .env
#
# Or export them inline if you prefer not to keep a .env file:
#   export UID=$(id -u) GID=$(id -g)

UID=1000
GID=1000
